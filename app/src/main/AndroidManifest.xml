<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 12L 旧的读写 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <!-- 13 精细媒体权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- 14 选择部分媒体 -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- 适配（暂不开启） Android 10 分区存储 ScopedStorage, 暂时保留旧版存储行为, targetSdk 30 运行在 Android 11+ (API 30+) 的设备上失效
    android:requestLegacyExternalStorage="true"
    -->
    <!-- WebView 支持明文流量 即 Http
    android:usesCleartextTraffic="true"
    -->
    <!-- ScopedStorage 卸载可保留数据
    android:hasFragileUserData="true"
    -->
    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:manageSpaceActivity=".presentation.feature.setting.ManageSpaceActivity"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="33">

        <activity
            android:name=".presentation.compose.ComposeActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.compose.AccountActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.compose.XhsActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.feature.person.HistoryActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.feature.person.BookmarkActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.feature.setting.ManageSpaceActivity"
            android:exported="true" />

        <activity
            android:name=".presentation.feature.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.feature.MainActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.feature.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.feature.setting.SettingActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.web.SchemeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- 还可以匹配 host path pathPrefix pathPattern -->
                <data android:scheme="wanandroid" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:scheme="market" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.feature.web.WebActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.common.ArticleWebActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.qa.QaWebActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.search.SearchActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.like.ArticleLikeActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.home.HomeActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.square.SquareActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.qa.QaActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.project.ProjectActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".presentation.feature.subscribe.SubscribeActivity"
            android:exported="false"
            android:theme="@style/AppTheme.ActionBar" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:enabled="true"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/util_file_paths" />
        </provider>

        <!-- ImmersionBar -->
        <!--适配华为（huawei）刘海屏-->
        <meta-data
            android:name="android.notch_support"
            android:value="true" />
        <!--适配小米（xiaomi）刘海屏-->
        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" />

    </application>

</manifest>