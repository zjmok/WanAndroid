# Local development: copy this file to release.jks.properties and fill in real values
# CI: set environment variables instead
# - RELEASE_STORE_FILE_BASE64
# - RELEASE_STORE_PASSWORD
# - RELEASE_KEY_ALIAS
# - RELEASE_KEY_PASSWORD
# To encode your jks for CI:
# - Linux or macOS: `base64 -i release.jks | tr -d '\n' | pbcopy`
# - Windows PowerShell: `[Convert]::ToBase64String([IO.File]::ReadAllBytes("release.jks")) | Set-Clipboard`

# 本地开发: 复制本文件为 release.jks.properties，并填写真实值
# CI: 请改为配置环境变量
# - RELEASE_STORE_FILE_BASE64
# - RELEASE_STORE_PASSWORD
# - RELEASE_KEY_ALIAS
# - RELEASE_KEY_PASSWORD
# 将 release.jks 编码为 Base64 (用于 CI):
# - Linux or macOS: `base64 -i release.jks | tr -d '\n' | pbcopy`
# - Windows PowerShell: `[Convert]::ToBase64String([IO.File]::ReadAllBytes("release.jks")) | Set-Clipboard`

storeFile=release.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
