// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    apply from: "config_sign.gradle"

    // 1.3.72 | 1.3.x last version
    // 1.4.32 | 1.4.x last version
    // 1.5.32 | 1.5.x last version
    // 1.6.21 | 1.6.x last version
    // 1.7.21 | 1.7.x last version
    // 1.8.22 | 1.8.x last version
    // 1.9.25 | 1.9.x last version
    ext.kotlin_version = "1.9.25"

    dependencies {
//        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${kotlin_version}"
    }
}

plugins {
    //
    // AGP 与 Gradle 对应版本, AGP 与 Android Studio 对应版本（另外 AGP 与 Gradle 对应版本见 `gradle-wrapper.properties`）
    // AGP https://developer.android.com/build/releases/gradle-plugin
    // AGP Older Versions https://developer.android.com/build/releases/gradle-plugin#expandable-1
    // AS                               --> AGP
    // Giraffe | 2022.3.1               --> 3.2-8.1
    // Hedgehog | 2023.1.1              --> 3.2-8.2
    // Iguana | 2023.2.1                --> 3.2-8.3
    // Jellyfish | 2023.3.1             --> 3.2-8.4
    // Koala | 2024.1.1                 --> 3.2-8.5
    // Koala Feature Drop | 2024.1.2    --> 3.2-8.6
    // Ladybug | 2024.2.1               --> 3.2-8.7
    // Ladybug Feature Drop | 2024.2.2  --> 3.2-8.8
    // Meerkat | 2024.3.1               --> 3.2-8.9
    // Meerkat Feature Drop | 2024.3.2  --> 3.2-8.10
    // Narwhal | 2025.1.1               --> 3.2-8.11
    // Narwhal Feature Drop | 2025.1.2  --> 4.0-8.12
    //
    // API 与 AGP 对应版本
    // API Level https://developer.android.com/build/releases/gradle-plugin#api-level-support
    // API --> minimum AS   --> minimum AGP
    // 36  --> M            --> 8.9.0
    // 35  --> KFD          --> 8.6.0
    // 34  --> H            --> 8.1.1
    // 33  --> F            --> 7.2
    //
    // 3.6.4 | 3.x.x last version
    // 4.2.2 | 4.x.x last version
    // 7.4.2 | 7.x.x last version | 从 AGP 7.0.0 开始，构建 AAR 时不支持模块直接依赖本地 .aar 文件（不支持子模块包含本地 aar，请使用 maven 远程依赖）
    // 8.1.4 | 支持 AS G 的最高版本
    //
    id "com.android.application" version "8.1.4" apply false // 8.1 需要 AS G+
    id "com.android.library" version "8.1.4" apply false
//    id "com.android.application" version "8.10.1" apply false // 8.10 需要 AS MFD+
//    id "com.android.library" version "8.10.1" apply false

    // KGP https://kotlinlang.org/docs/gradle-configure-project.html
    // KGP 要对应 android.composeOptions.kotlinCompilerExtensionVersion
    // compose-kotlin https://developer.android.com/jetpack/androidx/releases/compose-kotlin
    // 1.3.2 -> kotlin 1.7.20
    // 1.4.8 -> kotlin 1.8.22
    // 1.5.15 -> kotlin 1.9.25
    id "org.jetbrains.kotlin.android" version "${kotlin_version}" apply false
    id "org.jetbrains.kotlin.jvm" version "${kotlin_version}" apply false

    // KSP https://github.com/google/ksp/releases
    id "com.google.devtools.ksp" version "${kotlin_version}-1.0.20" apply false
}